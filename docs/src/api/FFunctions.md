# Classical helper functions

This section previously mirrored the `CFunctions` reference and is now
integrated into the dedicated [`CFunctions`](@ref) page. The module continues to
provide helper utilities such as `isnumeric`, `iszero`, and `stringer`, which
are documented alongside the core types.
